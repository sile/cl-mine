[概要]
・端末上で動作するマインスイーパー
・CommonLisp (SBCLのみ)

[バージョン]
・0.0.2

[使い方]
・REPLから
   $ cd cl-mine-x.x.x
   $ sbcl
   > (require :asdf)
   > (asdf:load-system :mine)
   > (mine:game WIDTH HEIGHT BOMB_COUNT)

・コマンドから
   $ cd cl-mine-x.x.x
   $ make
   $ ./mine WIDTH HEIGHT BOMB_COUNT
